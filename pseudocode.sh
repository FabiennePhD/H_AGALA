grep glyco oxygens
n_go = get number of these glyco oxygens

for (( i=1 ; i<=$n_go; i++ )); do
    grep the indexes of the other atoms you need
    build bonds, angles and dih
    var=" idx 1 idx2 type b"
    add the relevant lines into the topology below each [ section ]

done
